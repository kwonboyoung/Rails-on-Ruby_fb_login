<h1>게시물 수정</h1>
<%= form_tag(post_path(@post.id), method:"put") do%>
    제목 : <%= text_field_tag(:title, @post.title)%><br>
    내용 : <%= text_area_tag(:content, @post.content, size:"24x6")%><br>
    <%= submit_tag("글쓰기")%>
<%end%>